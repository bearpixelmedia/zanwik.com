# Minimal Railway Environment Variables
# Copy these to your Railway project variables

NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration (Update with your Vercel frontend URL)
CORS_ORIGIN=https://client-lhduha3h7-byronmccluney.vercel.app
FRONTEND_URL=https://client-lhduha3h7-byronmccluney.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Optional: Database (Add these later when you set up MongoDB)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/umbrella_dashboard_prod

# Optional: Redis (Add these later when you set up Redis)
# REDIS_URL=redis://username:password@host:port 